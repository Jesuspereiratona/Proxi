// Ajustar a mano según el entorno hasta que la Fase 8 defina cómo se inyecta en el despliegue real
// (no hay paso de build que reemplace esto por variable de entorno todavía).
export const API_URL = 'http://localhost:3000/api/v1';
